language: python
services:
  - docker
python:
  - "2.7"
install:
  - make deps
script:
  - make test
  - make lint
  - make docker_build
  - make docker_push
deploy:
  provider: heroku
  app: salty-wildwood-30209 #nazwa z heroku
  api_key: ${HEROKU_API_KEY} #zmienna srodowiskowa
